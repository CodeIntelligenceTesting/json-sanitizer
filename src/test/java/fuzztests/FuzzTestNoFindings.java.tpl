package fuzztests;

import com.code_intelligence.jazzer.junit.FuzzTest;

import com.google.json.EvalMinifier;

// @FuzzTest public void testID(String input) { try { EvalMinifier.minify(input); } catch (Exception _e) {} }

public class FuzzTestNoFindings {
  <fuzz-test-functions>
}
